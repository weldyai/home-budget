{
  "buildCommand": "npm run build",
  "outputDirectory": "webapp/dist",
  "routes": [
    { "src": "/webhook", "dest": "/api/webhook" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "crons": [
    { "path": "/api/cron", "schedule": "0 8 * * 1" }
  ]
}
